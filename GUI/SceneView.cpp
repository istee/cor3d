#include "SceneView.h"

/*
SceneView::SceneView(QWidget* parent) {
    if (parent)
    {
        _scene_widget = new SceneGLWidget(parent);
        parent->layout()->addWidget(_scene_widget);
        //scene_editor->layout()->addWidget(_scroll_area);
    }
}
*/
